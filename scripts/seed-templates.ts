/**
 * Seed the 20+ launch invitation templates.
 * Implemented in Phase 9 (Templates) once the schema-engine + Template model land.
 */
console.log(
  "Template seeding is implemented in Phase 9 (Templates). " +
    "See docs/04-Folder-Structure.md → scripts/seed-templates.ts.",
);
